// 1-script.js

// Wait for the DOM to be fully loaded before accessing elements
$(document).ready(function () {
  // Use the JQuery API to select the <header> element and update its text color to red (#FF0000)
  $("header").css("color", "#FF0000");
});
